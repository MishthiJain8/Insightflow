{
  "framework": null,
  "installCommand": "pip install -r requirements.txt",
  "buildCommand": "python -m py_compile main.py",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/main.py"
    }
  ]
}
